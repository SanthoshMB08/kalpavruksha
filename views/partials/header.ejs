<header class="navbar">
  <a href="/" class="brand">
    <img src="/assets/logo/logo-desktop.svg" alt="Kalpavruksha Kalyana" class="brand-logo brand-logo-desktop">
    <img src="/assets/logo/logo-mobile.svg" alt="Kalpavruksha Kalyana" class="brand-logo brand-logo-mobile">
  </a>
  <button type="button" class="nav-toggle" id="navToggle" aria-label="Toggle navigation" aria-expanded="false"><span class="nav-toggle-icon" aria-hidden="true"><span></span><span></span><span></span></span></button>
  <div class="nav-drawer-overlay" id="navDrawerOverlay"></div>
  <nav id="navLinks">
    <div class="nav-drawer-header">
      <div class="nav-drawer-brand">
        <span class="nav-drawer-brand-mark">✦</span>
        <div>
          <strong>Kalpavruksha</strong>
          <span>Menu</span>
        </div>
      </div>
      <button type="button" class="nav-drawer-close" id="navDrawerClose" aria-label="Close menu"><i class="fa-solid fa-xmark"></i></button>
    </div>
    <a href="/#about"><%= t('nav.about') %></a>
    <a href="/#stories"><%= t('nav.successStories') %></a>
    <a href="/#contact"><%= t('nav.contact') %></a>
    <% if (currentUser) { %>
      <a href="/dashboard"><%= t('nav.findMatch') %></a>
      <a href="/saved-profiles"><%= t('nav.savedProfiles') %></a>
    <% } %>
    <div class="nav-actions nav-actions-mobile">
      <div class="lang-toggle lang-toggle-mobile">
        <a href="/lang/en" class="<%= currentLang === 'en' ? 'active' : '' %>">EN</a>
        <a href="/lang/kn" class="<%= currentLang === 'kn' ? 'active' : '' %>">ಕನ್ನಡ</a>
      </div>
      <% if (currentUser) { %>
        <span style="color:#f5d576; font-size:0.85rem;"><%= t('nav.hello') %>, <%= currentUser.name %></span>
        <a href="/logout" class="btn btn-outline-gold btn-sm"><%= t('nav.logout') %></a>
      <% } else { %>
        <a href="/login" class="btn btn-outline-gold"><%= t('nav.login') %></a>
        <a href="/register" class="btn btn-gold"><%= t('nav.registerNow') %></a>
      <% } %>
    </div>
  </nav>
  <div class="nav-actions nav-actions-desktop">
    <div class="lang-toggle lang-toggle-desktop">
      <a href="/lang/en" class="<%= currentLang === 'en' ? 'active' : '' %>">EN</a>
      <a href="/lang/kn" class="<%= currentLang === 'kn' ? 'active' : '' %>">ಕನ್ನಡ</a>
    </div>
    <% if (currentUser) { %>
      <span style="color:#f5d576; font-size:0.85rem;"><%= t('nav.hello') %>, <%= currentUser.name %></span>
      <a href="/logout" class="btn btn-outline-gold btn-sm"><%= t('nav.logout') %></a>
    <% } else { %>
      <a href="/login" class="btn btn-outline-gold"><%= t('nav.login') %></a>
      <a href="/register" class="btn btn-gold"><%= t('nav.registerNow') %></a>
    <% } %>
  </div>
</header>
<% if (typeof topBannerAds !== 'undefined' && topBannerAds && topBannerAds.length) { %>
  <div class="top-banner-ads">
    <span class="eyebrow">Sponsored</span>
    <% topBannerAds.forEach(function(ad) { %>
      <a href="<%= ad.target_url || '#' %>" target="_blank" rel="noopener" class="top-banner-ad-item">
        <img src="<%= fileUrl('ads', ad.image_name) %>" alt="<%= ad.ad_title %>">
      </a>
    <% }) %>
  </div>
<% } %>
<script nonce="<%= nonce %>">
  (function () {
    var toggle = document.getElementById('navToggle');
    var links = document.getElementById('navLinks');
    var overlay = document.getElementById('navDrawerOverlay');
    var closeBtn = document.getElementById('navDrawerClose');
    if (!toggle || !links) return;

    function openDrawer() {
      links.classList.add('open');
      toggle.classList.add('is-open');
      if (overlay) overlay.classList.add('open');
      document.body.classList.add('nav-drawer-locked');
      toggle.setAttribute('aria-expanded', 'true');
    }
    function closeDrawer() {
      links.classList.remove('open');
      toggle.classList.remove('is-open');
      if (overlay) overlay.classList.remove('open');
      document.body.classList.remove('nav-drawer-locked');
      toggle.setAttribute('aria-expanded', 'false');
    }

    toggle.addEventListener('click', function () {
      links.classList.contains('open') ? closeDrawer() : openDrawer();
    });
    if (overlay) overlay.addEventListener('click', closeDrawer);
    if (closeBtn) closeBtn.addEventListener('click', closeDrawer);
    links.querySelectorAll('a').forEach(function (a) {
      a.addEventListener('click', closeDrawer);
    });
  })();
</script>
